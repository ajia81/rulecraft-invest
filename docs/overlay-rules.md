# Overlay Rules

룰 병합(오버레이) 규칙 상세 명세.

## TODO

- [ ] 병합 우선순위 설명: `override > extend > base inherit`
- [ ] `merge_policy` 값별 동작 예시 (base / extend / override)
- [ ] 자산군별 룰이 base 룰을 어떻게 변경하는지 시각적 예시
- [ ] 동일 `rule_id` 충돌 시 처리 규칙
- [ ] `required_indicators` 병합 시 중복 처리 정책
- [ ] `conditions` 병합 시 `all_of` / `any_of` 결합 정책
- [ ] 병합 결과 검증 방법
